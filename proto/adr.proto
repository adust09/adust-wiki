syntax = "proto3";

package proto;
option go_package = "proto/adr";

service ADRService {
    rpc AnalyzeADR (ADRRequest) returns (ADRResponse);
}

message ADRRequest {
    string adr = 1;
}

message ADRResponse {
    string result = 1;
}
